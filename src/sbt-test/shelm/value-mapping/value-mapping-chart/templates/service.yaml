apiVersion: v1
kind: Service
metadata:
  name: {{ include "value-mapping-chart.fullname" . }}
  labels:
    {{- include "value-mapping-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "value-mapping-chart.selectorLabels" . | nindent 4 }}
