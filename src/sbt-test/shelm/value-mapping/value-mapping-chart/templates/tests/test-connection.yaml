apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "value-mapping-chart.fullname" . }}-test-connection"
  labels:
    {{- include "value-mapping-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "value-mapping-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
